# Fast profile for short interactive turns.
# Use with: codex --profile fast

model_reasoning_effort = "medium"
model_verbosity = "low"
tool_output_token_limit = 20000

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 900
